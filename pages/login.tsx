import Head from 'next/head';

export default function About() {
  return (
    <div>
      <Head>
        <title>Login</title>
        <meta name="Login" content="Login" />
      </Head>

      <main>
        <h1>About the app</h1>
      </main>
    </div>
  );
}
